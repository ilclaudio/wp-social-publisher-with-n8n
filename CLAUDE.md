# CLAUDE.md

Claude Code specific entry point.

Read `AGENTS_README.md` for shared agent rules and file map.


## Session bootstrap
Run this section only once at session start, unless the user explicitly asks to reload CLAUDE context.

1. Read `PROJECT.md`.
2. Read `GIT_WORKFLOW.md`.
3. Read `README.md`.
4. Report exactly: `Bootstrap completed` + the full list of files read (include `CLAUDE.md`, `PROJECT.md`, `GIT_WORKFLOW.md`, `README.md`).
